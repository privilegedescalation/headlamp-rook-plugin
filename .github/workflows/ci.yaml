name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:
  workflow_call:

jobs:
  ci:
    uses: privilegedescalation/.github/.github/workflows/plugin-ci.yaml@main
